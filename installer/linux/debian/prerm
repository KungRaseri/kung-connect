#!/bin/sh
# KungConnect Agent — pre-removal script.
set -e

if [ "$1" = "remove" ] || [ "$1" = "upgrade" ]; then
    if command -v systemctl >/dev/null 2>&1; then
        systemctl stop kungconnect-agent.service    || true
        systemctl disable kungconnect-agent.service || true
    fi
fi
